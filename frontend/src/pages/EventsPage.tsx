import { EventCard, SectionTitle } from "../components/site";
import { events } from "../lib/data";
export function EventsPage() { return <><section className="border-b border-[var(--border)] bg-white"><div className="shell py-16 md:py-24"><p className="eyebrow">Gatherings and action</p><h1 className="mt-4 max-w-3xl text-5xl font-semibold leading-[1.02] tracking-[-.05em]">Upcoming events, when they are confirmed.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-[var(--grey)]">Event records will move to the API in a later phase. These cards show the intended public format without inventing dates.</p></div></section><section className="shell py-20"><SectionTitle eyebrow="Upcoming events" title="Make room on the calendar." /><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{events.map((event) => <EventCard key={event.id} event={event} />)}</div><div className="mt-20"><SectionTitle eyebrow="Past events" title="The archive will grow here." description="No past events have been published yet." /></div></section></>; }
