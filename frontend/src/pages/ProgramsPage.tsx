import { Link, useParams } from "react-router-dom";
import { ArrowLeft, ArrowRight } from "lucide-react";
import { ProgramCard, SectionTitle } from "../components/site";
import { programs } from "../lib/data";

export function ProgramsPage() { return <><PageIntro /><section className="shell py-20"><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{programs.map((program) => <ProgramCard key={program.id} program={program} />)}</div></section></>; }
export function ProgramDetailPage() { const { id } = useParams(); const program = programs.find((item) => item.id === id); if (!program) return <section className="shell py-24"><SectionTitle eyebrow="Programme" title="Programme not found" description="This programme does not have a published record yet." /><Link to="/programs" className="button button-secondary mt-8"><ArrowLeft size={16} /> Back to programmes</Link></section>; return <><section className="border-b border-[var(--border)] bg-white"><div className="shell grid gap-10 py-14 md:grid-cols-2 md:items-center"><div><Link to="/programs" className="inline-flex items-center gap-2 text-sm font-bold text-[var(--crimson)]"><ArrowLeft size={15} /> All programmes</Link><p className="eyebrow mt-10">{program.category}</p><h1 className="mt-4 text-5xl font-semibold leading-[1.02] tracking-[-.05em]">{program.title}</h1><p className="mt-6 text-lg leading-8 text-[var(--grey)]">{program.description}</p></div><img src={program.image} alt="" className="aspect-[4/3] w-full rounded-[2rem] object-cover" /></div></section><section className="shell grid gap-12 py-20 md:grid-cols-[1.1fr_.9fr]"><div><SectionTitle eyebrow="About this programme" title="Designed to grow through verified community work." /><p className="mt-6 leading-8 text-[var(--grey)]">{program.impact}</p><h2 className="mt-10 text-2xl font-semibold">Activities</h2><ul className="mt-4 grid gap-3">{program.activities.map((activity) => <li key={activity} className="border-l-2 border-[var(--crimson)] pl-4 text-[var(--grey)]">{activity}</li>)}</ul></div><div className="bg-[var(--light)] p-7"><p className="eyebrow">Upcoming activity</p><h2 className="mt-3 text-2xl font-semibold">Details to be announced</h2><p className="mt-3 leading-7 text-[var(--grey)]">Official dates, registration information and partner details will appear here after confirmation.</p><Link to="/contact" className="button button-primary mt-7">Ask a question <ArrowRight size={16} /></Link></div></section></>; }
function PageIntro() { return <section className="border-b border-[var(--border)] bg-white"><div className="shell py-16 md:py-24"><p className="eyebrow">What we do</p><h1 className="mt-4 max-w-3xl text-5xl font-semibold leading-[1.02] tracking-[-.05em]">Programmes for health, preparedness and community care.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-[var(--grey)]">Explore the initiative's current programme areas. Published details are placeholders until the official API and records are connected.</p></div></section>; }
