import { FormEvent, useState, type ChangeEvent } from "react";
import { FormInput, FormNotice, SectionTitle } from "../components/site";
const amounts = ["500", "1,000", "5,000"];
const handleAmountChange = (event: ChangeEvent<HTMLInputElement>, setAmount: (value: string) => void) => setAmount(event.target.value);
export function DonatePage() { const [amount, setAmount] = useState("500"); const [submitted, setSubmitted] = useState(false); const handleSubmit = (event: FormEvent) => { event.preventDefault(); setSubmitted(true); }; return <><section className="border-b border-[var(--border)] bg-white"><div className="shell py-16 md:py-24"><p className="eyebrow">Support the work</p><h1 className="mt-4 max-w-3xl text-5xl font-semibold leading-[1.02] tracking-[-.05em]">Give thoughtfully. See clearly where things stand.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-[var(--grey)]">Online donation processing will be enabled after payment gateway configuration. This form does not process or record payments.</p></div></section><section className="shell grid gap-12 py-20 lg:grid-cols-[.8fr_1.2fr]"><div><SectionTitle eyebrow="Donation preferences" title="Choose a starting point." /><div className="mt-8 grid grid-cols-3 gap-3">{amounts.map((item) => <button type="button" key={item} onClick={() => setAmount(item)} className={`rounded-xl border p-4 font-bold ${amount === item ? "border-[var(--crimson)] bg-red-50 text-[var(--crimson)]" : "border-[var(--border)] bg-white"}`}>₹{item}</button>)}</div><label className="mt-4 grid gap-2 text-sm font-semibold">Custom amount<input className="field" inputMode="numeric" value={amount} onChange={(event) => setAmount(event.target.value)} placeholder="Enter amount" /></label><div className="mt-7 grid gap-3"><p className="text-sm font-bold">Frequency</p><div className="flex gap-3"><span className="rounded-full bg-[var(--dark)] px-4 py-2 text-xs font-bold text-white">One time</span><span className="rounded-full border border-[var(--border)] px-4 py-2 text-xs font-bold">Monthly</span></div></div></div><form onSubmit={(event) => { event.preventDefault(); setSubmitted(true); }} className="rounded-2xl border border-[var(--border)] bg-white p-6 sm:p-8"><p className="eyebrow">Donor details</p><div className="mt-6 grid gap-5"><FormInput label="Name" name="name" /><FormInput label="Email" name="email" type="email" /><FormInput label="Phone" name="phone" type="tel" /><label className="grid gap-2 text-sm font-semibold">Purpose<select className="field" defaultValue="general" name="purpose"><option value="general">General support</option><option value="blood">Blood donation</option><option value="disaster">Disaster relief</option><option value="health">Health camps</option></select></label></div><button type="submit" className="button button-primary mt-6">Continue to payment</button>{submitted && <FormNotice />}</form></section></>; }
